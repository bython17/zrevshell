# # Build script for the project
# TODO: Don't forget the timeouts

# parser.add_argument(
#     "--request-rate",
#     "-rr",
#     type=int,
#     required=False,
#     help="The time(in seconds) rate that the victim will send requests to the server",
#     default=1,
# )

# parser.add_argument(
#     "--max-reconnect-timeout",
#     type=int,
#     required=False,
#     help="The maximum time the victim should not send any requests to the server due to several reasons. This will only be used if there is a problem.",
#     default=1200,
# )

# parser.add_argument(
#     "--min-reconnect-timeout",
#     type=int,
#     required=False,
#     help="The minimum time the victim should not send any requests to the server due to several reasons. This will only be used if there is a problem.",
#     default=10,
# )
